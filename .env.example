# Optional: Add a Serper API key for higher quality web search results
# Get one free at https://serper.dev
SERPER_API_KEY=

# Note: The LLM API key, base URL, and model are configured via the
# Settings panel in the app UI and stored in your browser's localStorage.
